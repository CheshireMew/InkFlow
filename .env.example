# Optional: place InkFlow data outside the operating-system default directory.
INKFLOW_DATA_DIR=D:\Tools\InkFlowData

# Optional: API providers can read their secret from this environment variable.
# Provider URL, adapter and model are configured in InkFlow itself.
INKFLOW_API_KEY=
